import styled from "styled-components";

export const UnstyledButton = styled.button`
  cursor: pointer;
  padding: 0;
  margin: 0;
  border: 0;
  border-radius: 8px;
  background: #fff;
  color: inherit;
  outline-width: 0;
  line-height: inherit;
`;

export const UnstyledImage = styled.img`
  padding: 0;
  margin: 0;
  border: 0;
  background: #fff;
  color: inherit;
  outline-width: 0;
  line-height: inherit;
`;
